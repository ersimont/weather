import { enableProdMode } from '@angular/core';

export const environment: Env = {
  pwa: true,
  gaProperty: 'UA-148865234-1',
  logEvents: false,
  paintGraph: true,
  storeDevtools: false,
};

enableProdMode();
